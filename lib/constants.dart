import 'package:flutter/material.dart';

class AppConstants {
  static const hiveBoxName = "happy_box";
  static const dateFormat = "MMM dd yyyy";
}

class Palette {
  static Color? background = Colors.grey[850];
  static Color? lightShadow = Colors.grey[800];
  static Color? darkShadow = Colors.grey[900];
}
